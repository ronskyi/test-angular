export enum DeleteComponentStateEnum {
  INITIAL,
  WAITING_RESPONSE,
  SUCCESS,
  UNKNOWN_ERROR,
}

export enum CreateStateEnum {
  INITIAL,
  WAITING_RESPONSE,
  SUCCESS,
  UNKNOWN_ERROR,
}

export enum UpdateStateEnum {
  INITIAL,
  LOADING,
  LOADED,
  WAITING_RESPONSE,
  SUCCESS,
  UNKNOWN_ERROR,
}

export enum ListLoadingStateEnum {
  INITIAL,
  LOADING,
  SUCCESS,
  UNKNOWN_ERROR,
}
